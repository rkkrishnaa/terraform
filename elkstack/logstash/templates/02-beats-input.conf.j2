input {
  beats {
    port => {{ beats_service_port }}
    ssl => true
    ssl_certificate => "{{ logstash_cert_path }}/logstash-forwarder.crt"
    ssl_key => "{{ logstash_key_path }}/logstash-forwarder.key"
  }
}
